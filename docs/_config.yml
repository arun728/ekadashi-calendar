# Place this file in your /docs folder as _config.yml

# Use Slate which is a dark theme with a teal-ish header
theme: jekyll-theme-slate

# Remove site title/header text to avoid duplication with index.md content
title: ""
description: ""

# Hide the default header buttons
show_downloads: false
